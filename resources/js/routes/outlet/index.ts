import outlets from './outlets'
import outletTypes from './outlet-types'

const outlet = {
    outlets: Object.assign(outlets, outlets),
    outletTypes: Object.assign(outletTypes, outletTypes),
}

export default outlet